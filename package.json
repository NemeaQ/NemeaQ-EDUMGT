{
  "private": true,
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0",
    "autoprefixer": "^10.3.6",
    "editorconfig-checker": "^4.0.2",
    "html-minifier": "^4.0.0",
    "postcss": "^8.3.8",
    "postcss-color-hex-alpha": "^8.0.2",
    "postcss-csso": "^6.0.0",
    "postcss-import": "^14.0.2",
    "uglify-js": "^3.14.5",
    "eslint": "^8.7.0",
    "stylelint": "^14.3.0",
    "stylelint-order": "^5.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "chart.js": "^3.6.2",
    "milligram": "^1.4.1"
  },
  "scripts": {
    "start": "eleventy --serve --quiet",
    "editorconfig": "editorconfig-checker",
    "stylelint": "stylelint \"src/**/*.css\"",
    "eslint": "eslint src/**/*.js",
    "html": "node-w3c-validator -i ./dist/**/*.html --exclude ./dist/**/demos/** -f lint -evH",
    "test": "npm run editorconfig && npm run stylelint && npm run eslint",
    "build": "rm -rf dist && eleventy && gulp build",
    "deploy": "cd dist && rsync --archive --compress --delete . sch24perm@perm-school24.ru:~/desk/public_html/"
  }
}
